const fs = require('fs');
const checkPackageJson = require('./checkPackageJson');

const run = function() {
    console.log('======running toffeenut ==========');
    /**
     * load config
     * 
     */
}

module.exports = run;